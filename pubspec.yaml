name: connectivity_alert_widget
description: A Flutter package to handle connectivity in an elegant way, giving power to the user to display different widgets when there's or there isn't connectivity
version: 2.0.0
homepage: https://github.com/rodrigobastosv/connectivity_alert_widget

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  connectivity: ^3.0.0-nullsafety.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
